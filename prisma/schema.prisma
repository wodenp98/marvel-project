generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model heroes {
  id         Int    @id @default(autoincrement())
  classhero  String 
  imageUrl   String
  name       String
  stars      Int
}
